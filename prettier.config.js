const config = {
  tabWidth: 2,
  semi: true,
  trailingComma: "es5",
  bracketSpacing: true,
  bracketSameLine: true,
  arrowParens: "always",
  plugins: ["prettier-plugin-astro"],
};

export default config;
